import { NEXT_QUESTION } from '../actions/actions'

export default function questions(state = [], action) {
  switch(action.type) {
    case NEXT_QUESTION:
      debugger
      break;
  }
  return state;
}
